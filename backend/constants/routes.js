const API = "/api",
  AUTH = "/auth",
  LOGIN = "/login",
  REGISTER = "/register",
  IDENTITY = "/identity",
  LOGOUT = "/logout";

module.exports = {
  API,
  AUTH,
  LOGIN,
  REGISTER,
  IDENTITY,
  LOGOUT
};
