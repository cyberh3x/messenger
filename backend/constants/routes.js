const API = "/api",
  AUTH = "/auth",
  LOGIN = "/login",
  REGISTER = "/register",
  IDENTITY = "/identity",
  LOGOUT = "/logout",
  USER = "/user",
  CONTACTS = "/contacts";

module.exports = {
  API,
  AUTH,
  LOGIN,
  REGISTER,
  IDENTITY,
  LOGOUT,
  USER,
  CONTACTS,
};
