import TextFieldComponent from "@mui/material/TextField";
import React from "react";

const TextField = (props) => {
  return <TextFieldComponent variant="outlined" fullWidth {...props} />;
};

export default TextField;
